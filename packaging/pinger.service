[Unit]
Description=Pinger Time Management Daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/pinger
Restart=always
RestartSec=3

[Install]
WantedBy=graphical-session.target
